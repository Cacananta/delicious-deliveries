const defaultState = {
  placeholder: 'placeholder'
}

export default function SearchResultsReducers(state = defaultState, action) {
  switch(type) {
    case 'PLACEHOLDER': {
      return {

      }
    }
    //next case here
  default: {
    return state
    }
  }
}