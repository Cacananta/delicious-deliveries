const defaultState = {
  placeholder: 'placeholder'
}

export default function RestaurantDetailsReducers(state = defaultState, action) {
  switch(type) {
    case 'PLACEHOLDER': {
      return {

      }
    }
    //next case here
  default: {
    return state
    }
  }
}